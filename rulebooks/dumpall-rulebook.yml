---
- name: Inspect incoming webhook payload
  hosts: all
  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 5000
  rules:
    - name: Print full event
      condition: event.payload is defined
      action:
        debug:
